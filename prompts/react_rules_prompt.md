let's create inside instructions/react.instructions.md file the rules for code standards
for this project. In this file we should: all the code must be written in english,
Purity in Components and Hooks,Components must be idempotent, Side effects must run outside of render, Props and state are immutable, Return values and arguments to Hooks are immutable, Values are immutable after being passed to JSX,Never call component functions directly,Never pass around hooks as regular values,Only call Hooks at the top level,Only call Hooks from React functions,Use Functional Component,Use TypeScript — files must use `.tsx` extension,Keep State as Close as Possible to Where It Is Used,Pass Props Explicitly,Avoid Large Components — split by responsibility,Use Tailwind for Styling,Use `useMemo` to Avoid Expensive Computations,Prefix Custom Hooks with `use`. Remember to create examples for each of this
rules and also to bind this rule (react.instructions.md) to the AGENTS.md file
